{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Agregar Cuenta
{% endblock %}

{% block content %}
    <h3 class="text-primary mb-4">Agregar cuenta</h3>
    <div class="row mb-2">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-block">
                    {{ form(agregarc_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}