{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} Agregar Cuenta Tercero
{% endblock %}

{% block content %}
    <h3 class="text-primary mb-4">Agregar cuenta de terceros</h3>
    <div class="row mb-2">
        <div class="col-lg-6">
            <div class="card">
                <div class="card-block">
                    {{ form(agregart_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}